__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.DS_Store

# artifacts / large files — regenerable
runs/
data/trajectron/
external/
results/
checkpoints/
*.avi
*.mp4

# The Grand Central (gc) raw data is 12,000+ per-frame files, so it is not kept
# in the repo. It is an optional dataset not needed by the main KoopCast++
# benchmark (ETH/UCY + snu-asri); the gc loader (kpp/data/loaders.py) is still
# present, so it works once you drop the OpenTraj originals in here.
data/raw/gc/

# Exception: our model artifacts are small, so we version them along with the code
!kpp/predictors/koopcastpp/data/*.pt
